<!-- <h1>Users</h1>

  <% @users.each do |u| %>
  <%= link_to "#{u.name}", user_path(u.id) %><br>
  <%= u.email %>
<% end %> -->

<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
    <li>
      <%= render @users %>
    </li>
</ul>
